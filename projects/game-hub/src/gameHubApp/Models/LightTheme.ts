import type {Theme} from "@/GameHubApp";

const LightTheme: Theme = {
    backgroundColor: "white", fontColor: "black", name: "Light Theme", themeStyleClass: "light-theme"
}
export default LightTheme;